// Optional default endpoint for the AI tutor.
// After deploying /worker, paste its /api/chat URL here OR save it from Reading settings in the site.
window.BEGINNER_BOOKS_CONFIG = {
  aiEndpoint: "https://YOUR-WORKER.YOUR-SUBDOMAIN.workers.dev/api/chat"
};
